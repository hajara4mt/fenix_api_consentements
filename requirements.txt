# === API FastAPI (déployée sur Azure App Service Plan) ===
fastapi==0.137.1
uvicorn[standard]==0.49.0
gunicorn==26.0.0

# === Azure SDK (auth managed identity + accès ADLS) ===
azure-identity==1.19.0
azure-storage-blob==12.24.0

# === Data (lecture/écriture parquet) ===
pyarrow==18.1.0
pandas==2.2.3
python-dateutil==2.9.0

# === Enedis : tables Delta Lake (vendoré depuis pipeline_enedis_F1) ===
deltalake>=0.18.0,<2.0.0

# === Tests (dev seulement) ===
pytest==8.3.4
pytest-mock==3.14.0
